import React from 'react'
import AuthProcessLayouts from '../../components/common/AuthProcessLayouts/AuthProcessLayouts'

const RolePicker = () => {
  return (
    <AuthProcessLayouts/>
  )
}

export default RolePicker
